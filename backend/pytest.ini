[pytest]
testpaths = tests
pythonpath = .
addopts = -q --tb=short
markers =
    slow: exercises the real speech models; skipped when they aren't downloaded
filterwarnings =
    ignore::DeprecationWarning
